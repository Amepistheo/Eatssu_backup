package com.example.eatssu;

import android.view.View;
import android.widget.EditText;
import android.widget.TextView;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.recyclerview.widget.RecyclerView;
/*
public class RestaurantHolder extends RecyclerView.ViewHolder {
        private final EditText mContext;
        private final TextView mPrice;
        private final EditText mRate;

        public RestaurantHolder(@NonNull View itemView) {
            super(itemView);
            mContext = itemView.findViewById(R.id.item_edt_contents);
            mPrice = itemView.findViewById(R.id.item_edt_price);
            mRate = itemView.findViewById(R.id.item_edt_rate);
        }

        public void bind(@NonNull Restaurant restaurant) {
            setTime(restaurant.getTime());
            setDate(restaurant.getDate());
        }

        private void setTime(@Nullable String time) {
            mTime.setText(time);
        }

        private void setDate(@Nullable String text) {
            mTextField.setText(text);
        }

}*/
